version: '3.8'

services:
  fastapi:
    build: .
    image: fastapi-cicd-pipeline:latest
    container_name: fastapi_app
    ports:
      - "80:8000"         # mapea el puerto 80 del host al 8000 del contenedor (opcional si usas un balanceador o proxy inverso)
    restart: unless-stopped